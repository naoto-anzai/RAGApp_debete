# Debate RAG (debate_agent) 依存パッケージ
# このマシンで動作確認済みのバージョンで固定(Python 3.11 前提)
# 別環境での再現用。GPU利用時のみ torch は環境に合わせて入れ直すこと(README参照)。

# --- LangChain / LangGraph ---
langgraph==1.2.10
langchain-groq==1.1.3
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# --- ベクトルDB / 埋め込み ---
chromadb==1.5.9
sentence-transformers==5.6.1
transformers==5.14.1
torch==2.13.0

# --- PDF読み込み ---
pypdf==6.14.2

# --- LLM(Groq)/ ユーティリティ ---
groq==0.37.1
python-dotenv==1.2.2
numpy==2.0.0
